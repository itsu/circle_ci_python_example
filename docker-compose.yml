version: "3.9"
   
services:
  db:
    image: postgres
    container_name: db
    hostname: postgres
    environment:
      - POSTGRES_NAME=my_media
      - POSTGRES_USER=example
      - POSTGRES_PASSWORD=1234
    ports:
      - 5432:5432
