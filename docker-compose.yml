version: "3.9"
   
services:
  db:
    image: postgres
    environment:
      - POSTGRES_NAME=my_media
      - POSTGRES_USER=example
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
